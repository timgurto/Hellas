name: Tests

on:
  push:
    branches: ['**']
  pull_request:
    branches: [master]

jobs:
  test:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Add MSBuild to PATH
        uses: microsoft/setup-msbuild@v2

      - name: Generate version header
        run: echo '#define VERSION "ci"' > src/version.h

      - name: Build tests
        run: msbuild Hellas.sln /t:test /p:Configuration=Debug /verbosity:minimal

      - name: Copy debug runtime DLLs
        run: |
          $vsPath = & "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" -latest -property installationPath
          $debugCrt = Get-ChildItem "$vsPath\VC\Redist" -Recurse -Filter "msvcp140d.dll" | Where-Object { $_.FullName -match 'x86' } | Select-Object -First 1
          $crtDir = $debugCrt.DirectoryName
          Write-Output "Found debug CRT at: $crtDir"
          Copy-Item "$crtDir\msvcp140d.dll" .
          Copy-Item "$crtDir\vcruntime140d.dll" .
          Copy-Item "C:\Windows\SysWOW64\ucrtbased.dll" .

      - name: Run tests
        run: .\test-debug.exe
